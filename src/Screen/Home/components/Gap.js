import React from 'react'
import { View } from 'react-native'

const Gap = ({width, height}) => {
    return (
        <View style={{width, height, backgroundColor: '#c0c1c1'}}/>
    )
}

export default Gap
